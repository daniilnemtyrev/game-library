export const searchSelector = (state) => state.filters.search;
export const orderFilterSelector = (state) => state.filters.orderFilter;

export const platformFilterSelector = (state) => state.filters.platformsFilter;
